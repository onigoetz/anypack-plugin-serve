{
  "name": "anypack-plugin-serve-aggregator",
  "version": "0.1.0-alpha.2",
  "description": "A Development Server in a Webpack Plugin",
  "license": "MPL-2.0",
  "scripts": {
    "test": "yarn workspaces foreach --all -pt run test",
    "ci:test": "yarn workspaces foreach --all -pt run ci:test",
    "lint-staged": "lint-staged",
    "build": "yarn workspaces foreach --all -t run build",
    "format": "yarn workspaces foreach --all -pt run format",
    "lint": "yarn workspaces foreach --all -pt run lint",
    "ci:lint": "yarn workspaces foreach --all -pt run ci:lint"
  },
  "private": true,
  "devDependencies": {
    "@biomejs/biome": "2.3.11",
    "lint-staged": "^16.2.7",
    "pre-commit": "^1.2.2"
  },
  "lint-staged": {
    "*.{js,mjs}": [
      "biome check --write --no-errors-on-unmatched --files-ignore-unknown=true --colors=off"
    ]
  },
  "workspaces": [
    "plugin",
    "plugin-overlay",
    "rspack-nano",
    "recipes/*"
  ],
  "pre-commit": "lint-staged",
  "packageManager": "yarn@4.9.2"
}
